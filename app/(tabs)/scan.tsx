import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function ScanScreen() {
  return (
    <PhasePlaceholder
      phase="Phase 3 – 4"
      title="Scan"
      description="Barcode scanning arrives in Phase 3. Nutrition label and meal photo recognition follow in Phase 4, each with a confirm step before anything is saved."
    />
  );
}
