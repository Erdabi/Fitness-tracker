import { PhasePlaceholder } from '@/components/PhasePlaceholder';

export default function TrainScreen() {
  return (
    <PhasePlaceholder
      phase="Phase 5"
      title="Train"
      description="Workout templates, the active session screen with a rest timer, and full training history."
    />
  );
}
